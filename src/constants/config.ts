/**
 * Global application configuration. 
 * Used primarily in the Header, Footer, and meta data.
 */
import type { AppConfig } from '../types';

export const APP_CONFIG: AppConfig = {
  appName: 'Ratio Lab',
  author: 'PytatoDuck',
  githubUrl: 'https://github.com/PytatoDuck/ratio-lab',
  version: '1.0.0',
};